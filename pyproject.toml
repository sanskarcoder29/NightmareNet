[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nightmarenet"
version = "0.5.0"
description = "Autonomous AI Self-Improvement Platform — Sleep-Inspired Training Paradigm"
readme = "README.md"
license = {text = "Apache-2.0"}
requires-python = ">=3.9"
authors = [
    {name = "Adit Jain", email = "aditjain@srm.edu.in"},
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Operating System :: OS Independent",
]
keywords = ["ai", "training", "robustness", "distortion", "machine-learning", "self-improvement"]

dependencies = [
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "transformers>=4.30.0",
    "datasets>=2.14.0",
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "scikit-learn>=1.3.0",
    "pyyaml>=6.0",
    "tqdm>=4.65.0",
    "beautifulsoup4>=4.12.0",
    "requests>=2.31.0",
    "pydantic>=2.0",
    "regex>=2023.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4.0",
    "mypy>=1.8.0",
    "mypy-baseline>=0.7.0",
    "types-PyYAML>=6.0",
    "hypothesis>=6.100.0",
]
api = [
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "httpx>=0.25.0",
    "python-multipart>=0.0.7",
    "slowapi>=0.1.9",
    "python-dotenv>=1.0",
]
tracking = [
    "wandb>=0.16.0",
    "tensorboard>=2.14.0",
]
mlflow = ["mlflow>=2.0"]
attacks = ["textattack>=0.3.8"]
hpo = ["optuna>=3.0.0"]
distributed = [
    "accelerate>=0.25.0",
]
otel = [
    "python-json-logger>=2.0.7",
    "opentelemetry-api>=1.24.0",
    "opentelemetry-sdk>=1.24.0",
    "opentelemetry-exporter-otlp-proto-grpc>=1.24.0",
]
hosted = [
    "sqlalchemy>=2.0.0",
    "alembic>=1.13.0",
    "authlib>=1.3.0",
    "celery>=5.3.0",
    "redis>=5.0.0",
    "pyjwt>=2.8.0",
    "psycopg[binary]>=3.1.0",
]
search = [
    "sentence-transformers>=2.2.0",
    "faiss-cpu>=1.7.0",
]
adaption = [
    "adaption>=0.1.0",
]
ai = [
    "openai>=1.30.0",
]
hub = [
    "huggingface_hub>=0.20.0"
]
compliance-pdf = [
    "reportlab>=4.0.0",
    "pyhanko>=0.23.0",
    "markdown>=3.5.0",
]
export = [
    "onnx>=1.14.0",
    "onnxruntime>=1.16.0"
]
art = [
    "adversarial-robustness-toolbox>=1.15.0",
]
benchmark = [
    "fvcore>=0.1.5",
]

[project.urls]
Homepage = "https://github.com/Adit-Jain-srm/NightmareNet"
Repository = "https://github.com/Adit-Jain-srm/NightmareNet"
Issues = "https://github.com/Adit-Jain-srm/NightmareNet/issues"

[project.scripts]
nightmarenet = "nightmarenet.cli:main"
nightmarenet-train = "scripts.train:main"
nightmarenet-generate = "scripts.generate_data:main"
nightmarenet-evaluate = "scripts.evaluate:main"
nightmarenet-reindex-search = "nightmarenet_server.search.reindex:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short -m 'not slow' --ignore=tests/test_distortion_vision.py"
markers = [
    "slow: marks tests as slow (deselect with '-m not slow')",
]

[tool.ruff]
target-version = "py39"
line-length = 100
exclude = [
    ".windsurf",
    ".claude",
    ".trae",
    ".agent",
    ".codebuddy",
    ".codex",
    ".continue",
    ".cursor",
    ".gemini",
    ".github/prompts",
    ".kiro",
    ".opencode",
    ".qoder",
    ".roo",
    "notebooks",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B"]
ignore = ["UP007", "UP006", "UP035", "UP045"]

[tool.mypy]
# Strict mode is the baseline going forward: it is what every *new* file is
# held to. Pre-existing violations in older files are frozen in
# mypy_baseline.txt (see docs/development/code-style.md) rather than
# loosened here, so this section should not be relaxed again without
# updating that document.
python_version = "3.12"
strict = true
warn_unused_configs = true
ignore_missing_imports = true
# import-untyped: redundant with ignore_missing_imports for our third-party
#   surface (torch/transformers/etc. ship no stubs); operator: torch's
#   dynamically-typed Tensor arithmetic makes this rule mostly noise here.
disable_error_code = ["import-untyped", "operator"]

[[tool.mypy.overrides]]
module = ["torch.*", "transformers.*", "datasets.*", "tqdm.*", "sklearn.*"]
follow_imports = "skip"
follow_imports_for_stubs = false

[[tool.mypy.overrides]]
module = [
    "nightmarenet.phases.*",
    "nightmarenet.compliance.*",
    "nightmarenet.hub.*",
    "nightmarenet.evaluation.certification",
    "nightmarenet.optimization.*",
    "nightmarenet.data.*",
]
disallow_any_generics = true
disallow_subclassing_any = true
disallow_untyped_calls = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
disallow_untyped_decorators = true
warn_unused_ignores = true
warn_return_any = true
no_implicit_reexport = true
strict_equality = true
strict_concatenate = true

[tool.setuptools.packages.find]
include = ["nightmarenet*", "nightmarenet_server*", "scripts"]
[tool.setuptools.package-data]
nightmarenet = ["py.typed"]
